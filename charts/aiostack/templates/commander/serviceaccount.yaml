{{- if .Values.commander.enabled }}
{{- if .Values.commander.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.commander.serviceAccount.name | default "aiostack-outpost-sa" }}
  namespace: {{ .Release.Namespace }}
  {{- with .Values.commander.serviceAccount.labels }}
  labels:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.commander.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}