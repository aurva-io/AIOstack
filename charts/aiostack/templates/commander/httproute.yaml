{{- if .Values.commander.enabled }}
{{- if .Values.commander.httproute.enabled }}
apiVersion: gateway.networking.k8s.io/v1beta1
kind: HTTPRoute
metadata:
  name: {{ .Values.commander.httproute.name }}
  labels:
    gateway: {{ .Values.commander.httproute.gateway.name }}
spec:
  parentRefs:
    - name: {{ .Values.commander.httproute.gateway.name }}
      namespace: {{ .Values.commander.httproute.gateway.namespace }}
  hostnames:
    - "{{ .Values.commander.httproute.host }}"
  rules:
    - backendRefs:
        - name: {{ .Values.commander.httproute.service.name }}
          port: {{ .Values.commander.httproute.service.port }}
{{- if .Values.commander.httproute.privateAccess.enabled }}
---
apiVersion: networking.gke.io/v1
kind: GCPBackendPolicy
metadata:
  name: {{ .Values.commander.httproute.name }}-private-vpn-access
spec:
  default:
    securityPolicy: {{ .Values.commander.httproute.privateAccess.securityPolicy }}
  targetRef:
    group: ""
    kind: Service
    name: {{ .Values.commander.httproute.service.name }}
{{- end }}
{{- end }}
{{- end }}