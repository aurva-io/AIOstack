{{- if .Values.observer.enabled }}
{{- if .Values.observer.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.observer.serviceAccount.name | default "aiostack-outpost-sa" }}
  namespace: {{ .Release.Namespace }}
  {{- with .Values.observer.serviceAccount.labels }}
  labels:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.observer.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
{{- end }}