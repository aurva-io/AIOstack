apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aiostack-docs.fullname" . }}-test-connection"
  labels:
    {{- include "aiostack-docs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aiostack-docs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
